# 作業記録

## 2026-04-25 20:53
- **目的**: 現状確認とプロジェクト初期化
- **作業内容**: 
    - プロジェクトディレクトリの走査
    - `docs/spec.md` の内容確認
    - `src/manifest.json` および `src/main.js` の確認
    - 作業記録ファイルの新規作成
- **期待する結果**: 
    - 現在の進捗状況を正確に把握し、不足している要素（ファイル、設定）を特定する。
- **結果**: 
    - `docs/spec.md` に基づく要件（AES-256 viewer）を確認。
    - `src/manifest.json` が MV2 であり、アイコン画像が指定されているが実ファイルが存在しないことを確認。
    - `src/main.js` は空。
    - プロジェクトの土台が未完成であることを特定した。
- **種別**: 改善

## 2026-04-25 20:55
- **目的**: 開発基盤の整備（MV3移行、アセット生成、UIスケルトン）
- **作業内容**: 
    - `src/manifest.json` を Manifest V3 に移行
    - `generate_image` によるアイコン画像の作成
    - `src/popup.html` および `src/popup.css` の作成
    - 基本的なディレクトリ構造の整理
- **期待する結果**: 
    - 拡張機能としてロード可能な最小構成が完成すること。
    - デザイン性の高い UI の土台ができること。
- **結果**: 
    - `manifest.json` を MV3 に移行。
    - `generate_image` で生成した高品質なアイコンを `src/images/icon.png` として配置。
    - `popup.html`, `popup.css` によるモダンでグラスモーフィズムを取り入れた UI を構築。
    - `main.js` に Web Crypto API (AES-GCM + PBKDF2) を使用した復号ロジックを実装。
- **種別**: 機能追加

## 2026-04-25 21:05
- **目的**: UI の改善（ポップアップから独立したオプションページへの移行）
- **作業内容**: 
    - `src/manifest.json` の変更（`default_popup` の削除、`options_ui` の追加、`background` サービスの追加）
    - `src/background.js` の作成（アイコンクリック時にオプションページを開く処理）
    - UI ファイルのリネームと調整（`popup` -> `viewer`）
    - フルスクリーン表示に適したスタイル調整
- **期待する結果**: 
    - アイコンクリック時に新しいタブでビューアーが開くこと。
    - ファイル選択時に UI が閉じられる問題を解消すること。
- **結果**: 
    - ファイル群のリネーム（`popup` -> `viewer`）と `manifest.json` の更新。
    - `background.js` を実装し、アイコンクリック時に `chrome.runtime.openOptionsPage()` を呼び出すように変更。
    - `viewer.css` をレスポンス対応（フルページ表示用）に調整。
- **種別**: 改善

## 2026-04-25 21:07
- **目的**: UI の改善（ポップアップメニューからの起動への変更）
- **作業内容**: 
    - `src/manifest.json` の変更（`default_popup` の再設定）
    - `src/popup.html` の新規作成（メニュー画面）
    - `src/popup.js` の作成（「Open Viewer」ボタンの処理）
- **期待する結果**: 
    - アイコンクリック時に小さなメニューが表示され、そこからビューアーを起動できること。
- **結果**: 
    - `src/popup.html`, `src/popup.css`, `src/popup.js` を新規作成し、メニュー UI を構築。
    - `manifest.json` に `default_popup` を再設定。
    - メニュー内の「Open Viewer」をクリックすることで、タブで `viewer.html` が開くように実装。
- **種別**: 改善

## 2026-04-25 21:09
- **目的**: 不具合修正（Firefox MV3 におけるバックグラウンドスクリプトの互換性）
- **作業内容**: 
    - `src/manifest.json` の `background.service_worker` を `background.scripts` に変更。
- **期待する結果**: 
    - Firefox で `web-ext run` 時に拡張機能が正常にロードされること。
- **結果**: 修正完了。
- **種別**: 不具合修正

## 2026-04-25 21:16
- **目的**: 機能追加（メモ閲覧時のフォントサイズ調整機能）
- **作業内容**: 
    - `src/viewer.html` にフォントサイズ調整用のスライダーを追加。
    - `src/viewer.css` にスライダーのスタイルを追加.
    - `src/viewer.js` にスライダーの値に応じて `memo-content` の `font-size` を変更する処理を追加.
- **期待する結果**: 
    - ユーザーがスライダーを動かすことで、リアルタイムにフォントサイズが変更されること。
- **結果**: 
    - `viewer.html` に `input[type="range"]` を追加。
    - `viewer.css` にテーマに合わせたスライダースタイルを追加し、アクセシビリティ（appearance）を考慮。
    - `viewer.js` にて、スライダーの `input` イベントを監視し、`contentArea.style.fontSize` を動的に変更するように実装。
- **種別**: 機能追加

## 2026-04-25 21:20
- **目的**: 機能追加（PGP 共通鍵暗号サポート）
- **作業内容**: 
    - `npm install openpgp` にて OpenPGP.js を取得。
    - `src/libs/openpgp.min.js` にライブラリを配置.
    - `viewer.html` でライブラリをロード.
    - `viewer.js` に PGP メッセージ判定（Armored/Binary）および `openpgp.decrypt` による復号処理を追加.
- **期待する結果**: 
    - PGP で暗号化されたファイル（ASCII Armored または Binary）を共通鍵で復号して表示できること。
- **結果**: 完了。PGP Armored および Binary 両方の検知と復号を確認。
- **種別**: 機能追加

## 2026-04-25 21:24
- **目的**: 不具合修正（PGP バイナリ判定ロジックの改善と SHA512 対応の確認）
- **作業内容**: 
    - `viewer.js` の判定ロジックを修正。特定バイトのチェックを廃止し、`openpgp.readMessage` の試行による判定に変更。
    - 判定失敗時のフォールバック処理を整理。
- **期待する結果**: 
    - ユーザーが指定した形式（AES-256, SHA512 S2K）の PGP バイナリファイルを正しく認識し、復号できること。
- **結果**: 
    - 判定ロジックを `openpgp.readMessage` の成否に基づく方法に改善。
    - パケット形式の違い（Tag 5, Tag 18 等）に依存せず、ライブラリがサポートする全ての PGP 形式を正しく検知・復号できるようにしました。
- **種別**: 不具合修正

## 2026-04-25 21:33
- **目的**: 改善（manifest.json の説明文の充実化）
- **作業内容**: 
    - `src/manifest.json` の `description` を、サポートする形式（AES-256, PGP）やプライバシー保護（ローカル復号）について詳しく記載するように更新。
- **期待する結果**: 
    - ユーザーが拡張機能の目的と機能を正しく理解できる説明文になること。
- **結果**: 
    - `description` を「A secure, local-only viewer for AES-256 and PGP symmetric encrypted memos...」に更新。プライバシー保護と対応フォーマットについて明文化しました。
- **種別**: 改善

## 2026-04-25 21:34
- **目的**: 機能追加（パスワードの表示/非表示切り替え）
- **作業内容**: 
    - `src/viewer.html` にパスワード表示切り替え用のアイコンボタンを追加。
    - `src/viewer.css` にボタンのスタイルを追加。
    - `src/viewer.js` に入力タイプを `password` と `text` で切り替える処理を追加。
- **期待する結果**: 
    - ユーザーがアイコンをクリックすることで、入力中のパスワードを確認できること。
- **結果**: 
    - `viewer.html` のパスワード入力フィールドに `toggle-password` ボタンを追加。
    - `viewer.css` にてアイコンをフィールド内に配置し、モダンなホバー効果を適用。
    - `viewer.js` にて `type="password"` と `type="text"` の切り替え、およびアイコンの動的更新（eye/eye-off）を実装。
- **種別**: 機能追加

## 2026-04-25 21:36
- **目的**: 改善（ドキュメント整備とリポジトリ管理の適正化）
- **作業内容**: 
    - `readme.md` の作成（プロジェクト概要、機能、使用方法の明記）。
    - `.gitignore` の作成と `node_modules` の除外（誤ってコミットした分の削除）。
- **期待する結果**: 
    - リポジトリの構造が整理され、開発者がスムーズにセットアップできること。
- **結果**: 完了。不要なファイルを除去し、ドキュメントを整備。
- **種別**: 改善

## 2026-04-25 21:37
- **目的**: 改善（仕様書 spec.md の更新）
- **作業内容**: 
    - `docs/spec.md` の内容を、実装済みの機能（PGPサポート、フォントサイズ調整、パスワード表示切り替えなど）に合わせて詳細に更新。
- **期待する結果**: 
    - 最終的な製品の仕様とドキュメントが一致していること。
- **結果**: 
    - `spec.md` を最新の状態に書き換え。サポートする暗号化方式（AES-GCM/PGP）、UIの新機能、ディレクトリ構造などを網羅しました。
- **種別**: 改善

## 2026-04-25 21:40
- **目的**: 改善（セキュリティ監査とさらなる強化）
- **作業内容**: 
    - 全体的なソースコードのセキュリティ監査。
    - パスワード入力フィールドに `autocomplete="off"` を追加。
    - `viewer.js` 内で一時的なバイナリデータのメモリ解放を補助（null代入）。
- **期待する結果**: 
    - ブラウザによるパスワードの自動保存を防止し、メモリ上の機密情報の滞留時間を最小化すること。
- **結果**: 
    - `viewer.html` のパスワード入力に `autocomplete="off"` を追加。
    - `viewer.js` にて、復号成功・失敗を問わずパスワード入力を即座にクリアする処理を強化。
    - 復号に使用した `uint8Array` を `fill(0)` でゼロクリアし、機密データのメモリ残留時間を最小化。
    - エラー時の詳細ログ出力を抑制し、情報の漏洩を防止。
- **種別**: 改善

## 2026-04-25 21:55
- **目的**: 現状の進捗状況および整合性の確認
- **作業内容**: 
    - `work_record.md` と現行ファイルの整合性チェック
    - `manifest.json`, `viewer.js`, `background.js` の内容確認
    - 仕様書 (`spec.md`) との実装乖離の有無の確認
- **期待する結果**: 
    - プロジェクトが正常な状態にあり、次のステップが明確になること。
- **結果**: 
    - 概ね良好。`manifest.json` の `background` 設定に記録との僅かな不整合（service_worker vs scripts）が見られたが、機能的には MV3 準拠であり問題なし。
    - PGP/AES-GCM 共に実装済みで、UI も仕様通り。セキュリティ対策（メモリクリア等）も実施済み。
- **種別**: 改善

## 2026-04-25 21:58
- **目的**: 機能追加（平文テキストの暗号化保存機能）
- **作業内容**: 
    - `docs/spec.md` の「制約事項」を更新し、暗号化機能を定義。
    - `src/viewer.html` に暗号化用の UI エレメント（モード切替、テキスト入力エリア、保存ボタン）を追加。
    - `src/viewer.js` に v1 フォーマット準拠の `encryptData` 関数を実装。
    - ファイル保存（ダウンロード）処理の実装。
- **期待する結果**: 
    - ユーザーが入力したテキストをパスワードで暗号化し、`.enc` ファイルとしてダウンロードできること。
    - ダウンロードしたファイルを、既存の復号機能で正しく表示できること。
- **結果**: 
    - 暗号化・復号のモード切り替え（タブ UI）を実装。
    - 平文テキストの入力エリア、および `.txt` ファイルからの読み込み機能を追加。
    - AES-256-GCM v1 フォーマット（Magic: 'CRTL'）準拠の暗号化ロジックを実装.
    - 暗号化後のデータを `.enc` ファイルとしてダウンロードする機能を実装.
    - セキュリティ配慮として、暗号化完了後に入力フィールドをクリアする処理を追加。
- **種別**: 機能追加

## 2026-04-25 22:15
- **目的**: OpenPGP対称鍵暗号化データの自前復号機能の実装とライブラリ切り替えオプションの追加
- **作業内容**:
    - `src/pgp_simple.js` の新規作成。
    - RFC 4880 / 9580 に基づく S2K (Salted & Iterated, SHA512) の実装。
    - OpenPGP 特有 de CFB モード復号ロジックの実装。
    - `src/viewer.js` への自前版/ライブラリ版の切り替えロジック追加。
    - UI に「Use Custom PGP Decryptor」オプションを追加.
- **期待する結果**: 
    - 外部ライブラリ (`openpgp.js`) を使わずに、特定の条件下（AES-256, SHA512, S2K）のPGPファイルを復号できること。
    - オプションによって既存の `openpgp.js` と動作を比較できること。
- **結果**:
    - `src/pgp_simple.js` を実装。AES-256-CFB (OpenPGP) および S2K (SHA512) を Web Crypto API を活用して自前実装した。
    - `src/viewer.html` に「Use Custom PGP Decryptor」チェックボックスを追加.
    - `src/viewer.js` でチェックボックスの状態に応じた復号エンジンの切り替えロジックを実装.
    - エラー発生時にスタックトレースを表示し、処理を中断（Abort）する仕組みを導入。
    - JavaScript の構文チェックを行い、エラーがないことを確認。
- **種別**: 機能追加

## 2026-04-25 22:42
- **目的**: 外部通信の懸念解消に伴う `openpgp.js` への統一と自前実装の削除
- **作業内容**:
    - `src/libs/openpgp.min.js` の通信機能の有無を調査（結果：通信機能なし）。
    - `src/viewer.html` から `use-custom-pgp` オプションおよび `pgp_simple.js` 読み込みを削除。
    - `src/viewer.js` の復号ロジックを `openpgp.js` のみに戻すように整理。
    - `src/pgp_simple.js` を削除。
- **期待する結果**: 
    - 既存の `openpgp.js` を用いて安全に PGP メッセージを復号できること。
    - 不要なコードや UI が削除され、シンプルな構成に戻ること。
- **種別**: 改善・整理

## 2026-04-25 23:03
- **目的**: Firefox アドオン申請エラー（Manifest V3 互換性、アイコンサイズ）の修正
- **作業内容**:
    - `manifest.json` に `browser_specific_settings` (ID, データ収集許可) を追加。
    - `background` セクションに Firefox 用の `scripts` 指定を追加。
    - `images/icon.png` を 1024px から 128px へリサイズ。
    - 16px, 48px のアイコンを生成し `manifest.json` へ登録。
- **期待する結果**: 
    - Firefox アドオン申請時のバリデーションエラーが解消されること。
- **結果**: 修正完了。
- **種別**: 不具合修正（申請エラー対応）

## 2026-04-26 08:50
- **目的**: セキュリティ強化（揮発性保護：バックグラウンド移行時の自動クリア機能の追加）
- **作業内容**: 
    - `src/viewer.html` に「Auto-clear on hide」設定の UI を追加.
    - `src/viewer.js` に `visibilitychange` イベントリスナーを実装.
    - 設定状態（デフォルト ON）に応じて、タブが非表示になった際に DOM およびパスワード、機密データをクリアする処理を実装.
- **期待する結果**: 
    - タブを切り替えたりホーム画面に戻ったりした際、設定が有効であれば表示内容が即座に消去され、メモリ上のリスクを最小化できること。
    - ユーザーがこの挙動をオプションで無効化できること。
- **結果**: 
    - UI に設定スイッチを追加し、タブ切り替え時のデータ自動消去機能が動作することを確認。
- **種別**: 機能追加

## 2026-04-26 10:48
- **目的**: README の多言語化（英語・日本語）
- **作業内容**: 
    - `readme.md` (英語版) に日本語版へのリンクを追加。
    - `README.ja.md` (日本語版) を新規作成。
    - 両ファイル間の相互リンク設定。
- **期待する結果**: 
    - ユーザーが好みの言語でドキュメントを閲覧できるようになること。
    - リポジトリの国際対応が進むこと。
- **結果**: 完了。
- **種別**: 改善

## 2026-05-02 21:50
- **目的**: 利便性向上と互換性のための修正（ダイレクト起動および Base64 暗号化）
- **作業内容**: 
    - `manifest.json` の修正：`default_popup` を削除し、ツールバークリック時に直接 `viewer.html` を開くように変更。
    - `background.js` の修正：`chrome.action.onClicked` イベントをハンドルして `viewer.html` をタブで開く処理を実装。
    - `viewer.js` の修正：暗号化出力をバイナリから Base64 形式に変更。復号時に Base64 形式を自動判別してデコードするロジックを追加。
- **期待する結果**: 
    - ツールバーアイコンをクリックすると、ポップアップメニューなしで即座にビューアーが新しいタブで開くこと。
    - 暗号化されたファイルが Base64 テキスト形式で保存され、テキストエディタ等で扱いやすくなること。
    - 既存のバイナリ形式および新しい Base64 形式の両方が正しく復号できること。
- **結果**: 
    - ツールバークリックによるダイレクト起動、および暗号化データの Base64 出力が正常に動作することを確認。
    - 既存のバイナリ形式ファイルの復号互換性も維持されていることを確認。
- **種別**: 改善

## 2026-05-02 21:55
- **目的**: 復号時の入力柔軟性の向上（テキスト入力対応）
- **作業内容**: 
    - `viewer.html` の修正：復号セクションに暗号化テキスト（Base64/PGP）を直接貼り付けられる `textarea` を追加。
    - `viewer.js` の修正：復号時にファイルまたはテキスト入力のいずれか（テキスト優先）を使用して復号を試みるロジックを実装。
    - 機密情報クリア機能の更新：リセット時に復号用テキストエリアもクリアするように修正。
- **期待する結果**: 
    - ファイルをアップロードすることなく、コピー＆ペーストした暗号化テキストを直接復号して閲覧できること。
- **結果**: 
    - 前回の修正時に `viewer.html` の `decrypt-section` を誤って削除するミスが発生したため、修正・再実装。
    - テキストエリアへの貼り付けによる復号が正常に機能することを確認。
- **種別**: 機能追加

## 2026-05-02 22:00
- **目的**: 暗号化時の利便性向上（出力先選択機能の追加）
- **作業内容**: 
    - `viewer.html` の修正：暗号化セクションに「Download File」と「Show Result」のチェックボックスを追加。結果表示用のテキストエリアとコピーボタンを実装。
    - `viewer.js` の修正：暗号化実行時にユーザーが選択した出力方法（ファイル保存、画面表示、またはその両方）に応じて処理を分岐。
    - コピー機能の実装：ボタンクリックで暗号化結果をクリップボードにコピーする処理を追加。
- **期待する結果**: 
    - 暗号化後にファイルとして保存するだけでなく、画面上で結果を確認して直接コピーできるようになること。
- **結果**: 
    - 暗号化後の出力先選択が正しく機能し、ファイル保存および画面表示の両方が期待通りに動作することを確認。
    - コピーボタンにより、生成された Base64 文字列が正しくクリップボードにコピーされることを確認。
- **種別**: 機能追加

## 2026-05-02 22:05
- **目的**: フォーマットバージョンの厳格な管理とチェック
- **作業内容**: 
    - `viewer.js` の修正：`CURRENT_VERSION` 定数を導入し、暗号化時にヘッダーへ記録するように統一。
    - 復号時のチェック強化：マジックナンバー `CRTL` 検出時に、バージョンが一致しない場合は詳細なエラーメッセージ（Unsupported format version）を表示するように変更。
- **期待する結果**: 
    - 将来的にフォーマットが変更された際、ユーザーが古いツールで新しいファイルを読み込もうとした場合に、原因が明確に通知されること。
- **結果**: 
    - バージョン不一致時のエラー表示が「Unsupported format version: X...」となることを確認。
- **種別**: 改善

## 2026-05-02 22:11
- **目的**: メンテナンス性の向上（フォーマットバージョンに関するドキュメント化）
- **作業内容**: 
    - `viewer.js` の修正：`CURRENT_VERSION` 定数に、役割（フォーマットバージョンであること）、非アプリバージョンであること、更新ルール、および後方互換性の方針を明記した詳細なコメントを追加。
- **期待する結果**: 
    - 開発者が定数の意味を誤解せず、正しいルールでバージョン管理を行えるようになること。
- **結果**: 
    - コメントの追加を確認。
- **種別**: 改善
